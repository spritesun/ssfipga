<% if messages.blank? %>
    <%= t('no_message') %>
<% else %>
    <% messages.each do |message| %>
        <div>
          <div><b>Title: </b><span><%= h message.title %></span></div>
          <div><b>Body: </b><span><%= h message.body %></span></div>
        </div>
    <% end %>
<% end %>